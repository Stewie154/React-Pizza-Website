import React from 'react';
import Navbar from '../navbar';
import { HeroContainer } from './heroElements';
const Hero = () => {
    return (
        <HeroContainer>
            <Navbar />
        </HeroContainer>
    )
}

export default Hero;
